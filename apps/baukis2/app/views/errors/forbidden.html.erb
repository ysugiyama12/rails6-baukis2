<div id="error">
    <h1>403 forbidden</h1>
    <p>
        <%= 
            case @exception
            when ApplicationController::IpAddressRejected
                "あなたのIPアドレス（#{request.ip}）からは利用できません。"

            else
                "指定したページを閲覧する権限がありません。"
            end
        %>
    </p>
    </div>